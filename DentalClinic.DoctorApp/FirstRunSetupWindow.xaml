<Window x:Class="DentalClinic.DoctorApp.FirstRunSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource FirstRun_Title}" Height="640" Width="440"
        Icon="pack://application:,,,/DentalClinic.UI;component/Assets/logo_icon.ico"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Border CornerRadius="18" Background="{StaticResource CardBackgroundBrush}">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.22" BlurRadius="30" ShadowDepth="6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- شريط عنوان بسيط: لا يوجد زر إغلاق عمداً - أول تشغيل يجب أن يُكمَل (لا يوجد حساب بعد) -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"/>

            <StackPanel Grid.Row="1" Margin="40,0,40,26" VerticalAlignment="Center">

                <Border Width="80" Height="80" CornerRadius="40" Background="White"
                        BorderBrush="{StaticResource AccentBrush}" BorderThickness="2.5"
                        HorizontalAlignment="Center" Margin="0,0,0,14">
                    <Image Source="pack://application:,,,/DentalClinic.UI;component/Assets/logo_icon.png"
                           Width="56" Height="56" Stretch="Uniform"/>
                </Border>

                <TextBlock Text="{DynamicResource FirstRun_Title}" FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}"
                           HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,6"/>

                <TextBlock Text="{DynamicResource FirstRun_Subtitle}" FontSize="12.5"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"
                           Margin="0,0,0,22"/>

                <TextBlock Text="{DynamicResource FirstRun_FullName}" Style="{StaticResource FieldLabelStyle}"/>
                <Border Style="{StaticResource InputContainerStyle}" Margin="0,0,0,14">
                    <TextBox x:Name="FullNameBox" Style="{StaticResource PlainTextBoxStyle}"/>
                </Border>

                <TextBlock Text="{DynamicResource FirstRun_Username}" Style="{StaticResource FieldLabelStyle}"/>
                <Border Style="{StaticResource InputContainerStyle}" Margin="0,0,0,14">
                    <TextBox x:Name="UsernameBox" Style="{StaticResource PlainTextBoxStyle}"/>
                </Border>

                <TextBlock Text="{DynamicResource FirstRun_Password}" Style="{StaticResource FieldLabelStyle}"/>
                <Border Style="{StaticResource InputContainerStyle}" Margin="0,0,0,14">
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource PlainPasswordBoxStyle}"/>
                </Border>

                <TextBlock Text="{DynamicResource FirstRun_ConfirmPassword}" Style="{StaticResource FieldLabelStyle}"/>
                <Border Style="{StaticResource InputContainerStyle}" Margin="0,0,0,6">
                    <PasswordBox x:Name="ConfirmPasswordBox" Style="{StaticResource PlainPasswordBoxStyle}"/>
                </Border>

                <TextBlock x:Name="ErrorText" Style="{StaticResource ErrorTextStyle}"/>

                <Button x:Name="CreateButton" Content="{DynamicResource FirstRun_CreateButton}" Height="46" Margin="0,16,0,0"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="CreateButton_Click" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
