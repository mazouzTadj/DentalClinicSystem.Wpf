<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:conv="clr-namespace:DentalClinic.UI.Converters">

    <!-- مُحوِّل عام متاح في كل نوافذ التطبيق لإصلاح الأقواس/الرموز في أي نص ديناميكي
         (غير قادم من ملفات الترجمة) قبل عرضه، مثال:
         Text="{Binding SomeDbText, Converter={StaticResource ArabicBidiConverter}}" -->
    <conv:ArabicBidiConverter x:Key="ArabicBidiConverter"/>

    <!-- شبكة أمان: في حال فُتحت أي نافذة قبل تحميل LocalizationManager.Initialize() لأي سبب،
         تبقى الواجهة بالاتجاه الافتراضي بدل أن يفشل XAML في العثور على المورد. يُستبدَل هذا القيمة
         فوراً بالقيمة الصحيحة (LeftToRight أو RightToLeft) بمجرد دمج Strings.en.xaml أو Strings.ar.xaml. -->
    <FlowDirection x:Key="AppFlowDirection">RightToLeft</FlowDirection>

    <!-- يُطبَّق تلقائياً على كل نافذة في التطبيق (من أي مشروع) طالما لم تُحدَّد Style صريحة عليها -->
    <Style TargetType="Window">
        <Setter Property="FlowDirection" Value="{DynamicResource AppFlowDirection}"/>
    </Style>

    <!-- ========== إعدادات الحركة الموحَّدة (Motion Design tokens) ==========
         مدد وEasing ثابتة عبر كل التطبيق - غيّرها هنا مرة واحدة تؤثر على كل مكان -->
    <Duration x:Key="MotionFast">0:0:0.12</Duration>
    <Duration x:Key="MotionNormal">0:0:0.18</Duration>
    <CubicEase x:Key="MotionEaseOut" EasingMode="EaseOut"/>

    <!-- ========== ألوان محايدة مشتركة بين التطبيقين ==========
         AccentBrush يُعرَّف داخل App.xaml الخاص بكل تطبيق (أزرق للاستقبال، أخضر للطبيب) -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1F2937"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#6B7280"/>
    <SolidColorBrush x:Key="BorderSoftBrush" Color="#DDE3EA"/>
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#F7F9FB"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#C0392B"/>
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#EEF2F6"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>

    <!-- حاوية مدوّرة الحواف لأي حقل إدخال - أضفنا توهّج (glow) ناعم عند التركيز -->
    <Style x:Key="InputContainerStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect x:Name="FocusGlow" Color="#2563EB" Opacity="0" BlurRadius="10" ShadowDepth="0"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                             To="0.35" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                             To="0" Duration="{StaticResource MotionFast}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PlainTextBoxStyle" TargetType="TextBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="PlainPasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="Margin" Value="4,0,0,4"/>
    </Style>

    <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,8,0,0"/>
        <Setter Property="MinHeight" Value="18"/>
    </Style>

    <!-- زر رئيسي مدوّر بلون التطبيق (Accent) - رفعة (lift) + انضغاطة (press) + ظل يتنفّس عند التحويم -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Bg" CornerRadius="10" Background="{TemplateBinding Background}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="BgScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect x:Name="BgShadow" Color="#000000" Opacity="0.0"
                                                   BlurRadius="16" ShadowDepth="4" Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Opacity" Value="0.92"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX"
                                                         To="1.02" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY"
                                                         To="1.02" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgShadow" Storyboard.TargetProperty="Opacity"
                                                         To="0.28" Duration="{StaticResource MotionNormal}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="{StaticResource MotionFast}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="{StaticResource MotionFast}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgShadow" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource MotionNormal}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bg" Property="Opacity" Value="0.8"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX" To="0.97" Duration="0:0:0.08"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY" To="0.97" Duration="0:0:0.08"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- زر ثانوي (Outline) لإجراءات أقل أهمية مثل "إلغاء" - نفس لغة الحركة بدون ظل قوي -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" CornerRadius="10" Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="1"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="BgScale" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#F6F2FC"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX" To="1.015" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY" To="1.015" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="{StaticResource MotionFast}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="{StaticResource MotionFast}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#EDE7F8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- زر دائري صغير لعناصر تحكم النافذة المخصّصة (زر الإغلاق X) -->
    <Style x:Key="WindowControlButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" CornerRadius="16" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#FDE8E8"/>
                            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- زر أيقونة صغير بدون إطار (تبديل حالة) - يُستخدم لإظهار/إخفاء كلمة المرور ولزر القائمة الجانبية -->
    <Style x:Key="IconToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Bg" CornerRadius="16" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#EEF2F6"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#E4ECF7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- زر تحذيري (أحمر) لإجراءات حساسة مثل تسجيل الخروج -->
    <Style x:Key="DangerButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" CornerRadius="10" Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource ErrorBrush}" BorderThickness="1"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="BgScale" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#FDEDED"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX" To="1.015" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY" To="1.015" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="{StaticResource MotionFast}"/>
                                        <DoubleAnimation Storyboard.TargetName="BgScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="{StaticResource MotionFast}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#FBDCDC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- زر افتراضي موحّد: أي Button لم يحدد Style صريحة يحصل تلقائياً على نفس لغة التصميم.
         هذا يحسّن النوافذ القديمة وعناصر التبويب بدون التأثير على Primary/Secondary/Danger الصريحة. -->
    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}"/>

    <!-- ========== جديد: بطاقة مرتفعة (Card) - اختيارية، لم تُطبَّق على أي نافذة قائمة تلقائياً.
         استخدمها يدوياً حين تريد: <Border Style="{StaticResource CardStyle}"> ... </Border> ========== -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#0F172A" Opacity="0.08" BlurRadius="20" ShadowDepth="4" Direction="270"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- بطاقة قابلة للنقر (نفس CardStyle + تفاعل تحويم واضح) - للعناصر القابلة للنقر مثل صف ملخّص -->
    <Style x:Key="InteractiveCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.01" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.01" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.14" Duration="{StaticResource MotionNormal}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="{StaticResource MotionFast}"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="{StaticResource MotionFast}"/>
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.08" Duration="{StaticResource MotionNormal}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ========== تبويبات موحّدة Modern Tabs ==========
         قالب آمن لـ WPF: يعتمد فقط على Border + ContentPresenter وربط Header مباشرة،
         لتجنب XamlParseException عند تحميل النوافذ التي تحتوي على TabControl. -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Padding" Value="16,9"/>
        <Setter Property="Margin" Value="0,0,4,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0,0,0,2"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter
                            Content="{TemplateBinding Header}"
                            ContentTemplate="{TemplateBinding HeaderTemplate}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="#F6F2FC"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="White"/>
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="TabBorder" Property="BorderThickness" Value="0,0,0,3"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== عنصر قائمة جانبية (Sidebar) بمؤشر نشاط متحرك عند التحويم ==========
         شريط رفيع بلون التطبيق ينزلق من الأعلى ويظهر عند التحويم فوق أي عنصر - يمنح إحساس
         تنقّل واضح حتى لو كانت العناصر أوامر مباشرة (وليست صفحات ثابتة) كما في هذا التطبيق -->
    <Style x:Key="SidebarMenuItemStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,0,10,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" CornerRadius="8" Background="{TemplateBinding Background}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Indicator" Grid.Column="0" Width="4" CornerRadius="2" Margin="0,6"
                                    Background="{DynamicResource AccentBrush}" Opacity="0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform x:Name="IndicatorScale" ScaleY="0"/>
                                </Border.RenderTransform>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left"
                                               Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#F1F5F9"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Indicator" Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="{StaticResource MotionFast}"/>
                                        <DoubleAnimation Storyboard.TargetName="IndicatorScale" Storyboard.TargetProperty="ScaleY"
                                                         To="1" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Indicator" Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="{StaticResource MotionFast}"/>
                                        <DoubleAnimation Storyboard.TargetName="IndicatorScale" Storyboard.TargetProperty="ScaleY"
                                                         To="0" Duration="{StaticResource MotionFast}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#E2E8F0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== ComboBox عصري - Style ضمني (بدون x:Key) يُطبَّق تلقائياً على كل ComboBox
         بالتطبيق دون الحاجة لتعديل أي من الملفات الستة التي تستخدمه حالياً ==========
         السهم يدور 180° بسلاسة عند الفتح، والقائمة المنسدلة بطاقة بيضاء مدوّرة بظل ناعم -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bg" CornerRadius="6" Margin="4,1" Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#F1F5F9"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#E4ECF7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="12,10,28,10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid x:Name="ComboRoot">
                        <!-- منطقة نقر شفافة بالكامل تغطي عرض الـComboBox كله - بلا أي محتوى بصري خاص بها -->
                        <ToggleButton x:Name="Toggle" Background="Transparent" BorderThickness="0" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <!-- السهم - عنصر شقيق مباشر داخل نفس قالب ComboBox (وليس متداخلاً بقالب آخر)،
                             حتى يستطيع Trigger القالب الخارجي أدناه إيجاده عبر TargetName بنفس نطاق الأسماء -->
                        <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0" Stroke="{StaticResource TextSecondaryBrush}"
                              StrokeThickness="1.6" HorizontalAlignment="Right" VerticalAlignment="Center"
                              Margin="0,0,10,0" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform x:Name="ArrowRotate" Angle="0"/>
                            </Path.RenderTransform>
                        </Path>

                        <!-- المحتوى الفعلي المعروض (العنصر المختار) - Toggle أعلاه شفاف بالكامل ويستخدَم فقط للتبديل -->
                        <ContentPresenter x:Name="SelectedContent" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" HorizontalAlignment="Left"/>

                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                            <Border Background="White" CornerRadius="8" Padding="4"
                                    MinWidth="{Binding ActualWidth, ElementName=ComboRoot}"
                                    MaxHeight="260">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" Opacity="0.16" BlurRadius="18" ShadowDepth="4"/>
                                </Border.Effect>
                                <ScrollViewer VerticalScrollBarVisibility="Auto" SnapsToDevicePixels="True">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ArrowRotate" Storyboard.TargetProperty="Angle"
                                                         To="180" Duration="{StaticResource MotionFast}" EasingFunction="{StaticResource MotionEaseOut}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ArrowRotate" Storyboard.TargetProperty="Angle"
                                                         To="0" Duration="{StaticResource MotionFast}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========== أنماط جدول البيانات (DataGrid) - نسخة مبسّطة وموثوقة ==========
         تراجعنا عمداً عن نسخة سابقة كانت تحرّك لون خلفية الصف عبر Storyboard: تسبّبت في تعطّل
         التحديد بنقرة واحدة على بعض الأجهزة رغم عمل تأثير hover. هذه النسخة تستخدم Setters
         فورية فقط ضمن Style.Triggers (بلا أي Storyboard وبلا أي ControlTemplate مخصَّص لا للصف
         ولا للخلية) - نفس الطريقة القياسية الموثوقة 100% في WPF، فقط بدون الانتقال اللوني الناعم. -->
    <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowHeight" Value="46"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#EEF1F4"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
    </Style>

    <Style x:Key="ModernColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#F1F5F9"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="#E2E8F0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>

    <Style x:Key="ModernRowStyle" TargetType="DataGridRow">
        <Setter Property="Padding" Value="0,4"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F5F8FA"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#E4F0FA"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ModernCellStyle" TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Style.Triggers>
            <!-- نص داكن ثابت وخلفية شفافة حتى عند التحديد، حتى يظهر لون خلفية الصف (ModernRowStyle) بوضوح -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- يُستخدم عبر ElementStyle صراحةً على كل DataGridTextColumn (وليس Style ضمني عام لـTextBlock -
         ذلك كان سيؤثر على كل نص بالتطبيق بلا استثناء). يضمن هامشاً حقيقياً داخل كل خلية نصّية،
         بغضّ النظر عن اعتماد Padding الخاص بـDataGridCell نفسه. -->
    <Style x:Key="CellTextStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="14,8"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>
